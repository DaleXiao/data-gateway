{
  "name": "data-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Unified data gateway for openclawd.co (CF Worker + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20241022.0",
    "vitest": "^1.5.0",
    "wrangler": "^3.70.0"
  }
}
