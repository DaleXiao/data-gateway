# memcare-site — F-2 source snapshot (SPEC-215)

⚠️ **Architecture note for Rei**: This is NOT a TS/React project.
- `build.sh` reads `memory/health/*.json` and injects data into `index.template.html`
- All logic is inline JS in `index.template.html` — no `src/`, no bundler
- `dist/` is generated by `build.sh` at deploy time

## F-2 changes needed
The `?v=2` flag + gateway fetch go directly into `index.template.html`.
Look for the data initialization section near `window.__DATA__` or `const days`.
The gateway URL is `https://data.openclawd.co/api/memcare/health` (no token needed client-side).

After changes, send Cindy a diff/patch → she applies to monorepo + runs build.sh to verify.
